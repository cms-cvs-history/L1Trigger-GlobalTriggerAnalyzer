module l1GtDataEmulAnalyzer = L1GtDataEmulAnalyzer {
 
    # input tag for the L1 GT hardware DAQ/EVM record
    InputTag L1GtDataInputTag = "gtDigis"
    
    # input tag for the L1 GT emulator DAQ/EVM record
    InputTag L1GtEmulInputTag = "l1GtEmulDigis"
    
    # input tag for the L1 GCT hardware record 
    InputTag L1GctDataInputTag = "gctDigis"

}
